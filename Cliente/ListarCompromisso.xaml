<Window x:Class="Cliente.ListarCompromisso"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cliente"
        mc:Ignorable="d"
        Title="ListarCompromisso" Height="266.4" Width="343.2">
    <Grid>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Left" Margin="10,10,-31.4,0" VerticalAlignment="Top" Height="199" Width="315" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding id}" Header="Id"/>
                <DataGridTextColumn Binding="{Binding descricao}" Header="Descricao"/>
                <DataGridTextColumn Binding="{Binding local}" Header="Local"/>
                <DataGridTextColumn Binding="{Binding data}" Header="Data"/>
                <DataGridTextColumn Binding="{Binding realizado}" Header="Realizado"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
